kind: pipeline
name: test

steps:
- name: docker-test
  image: docker:dind
  volumes:
    - name: dockersock
      path: /var/run
  commands:
    - echo ${DRONE_BUILD_NUMBER}

services:
  - name: docker
    image: docker:dind
    privileged: true
    volumes:
      - name: dockersock
        path: /var/run

volumes:
  - name: dockersock
    temp: {}